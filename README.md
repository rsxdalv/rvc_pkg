# RVC as an installable package (WIP)

## Installation

```bash
pip install rvc_pkg
```

## Usage

```python
import rvc_pkg
```

## Hopes and Dreams

That RVC Becomes installable by itself and also publishes a package on PyPi itself.
